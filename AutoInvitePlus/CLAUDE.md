# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

AutoInvite Plus is a **World of Warcraft 3.3.5a (Wrath of the Lich King) addon** — client Interface version `30300`. It is a raid-organization suite: keyword auto-invite, an LFM/LFG chat browser, raid composition advisor, queue/waitlist, blacklist/favorites, loot history, and an inter-player addon-comms layer (DataBus).

There is **no build, compile, lint, or test toolchain**. The code is interpreted Lua 5.1 loaded directly by the WoW client. "Running" the addon means launching WoW and `/reload`-ing the UI.

## Development workflow

- **Edit → `/reload` in-game → observe.** Lua errors surface in-game (use `/console scriptErrors 1` or an addon like BugSack). There is no way to run this outside the WoW client.
- **Load order is defined by `AutoInvitePlus.toc`**, not by `require`. Files load top-to-bottom: `core/` → `data/` → `modules/` → `ui/`. A file may only use sub-namespaces defined by files listed *above* it. If you add a `.lua` file, you **must** add it to the `.toc` or it will never load. `core/Utils.lua` loads first and seeds the global namespace.
- **In-game test data**: `/aip testdata` loads fake LFM/LFG/queue data for UI work without live chat; `/aip cleartest` removes it (preserves real data). `/aip testloot` / `/aip cleartestloot` do the same for loot history. The `modules/TestData.lua` module is the source of this fixture data.
- **Saved state**: `AutoInvitePlusDB` (declared `## SavedVariables` in the `.toc`) persists to the player's `SavedVariables` folder. To reset, delete that file or `/run AutoInvitePlusDB=nil` then `/reload`.

## Architecture

### Single global namespace
Every file shares one global table, `AutoInvitePlus` (aliased `local AIP = AutoInvitePlus` at the top of each file). Each module attaches its own sub-table, e.g. `AIP.Parsers`, `AIP.DataBus`, `AIP.Composition`, `AIP.CentralGUI`, `AIP.Panels.RaidMgmt`. There are no module imports — cross-module access is always `AIP.Something`, and calls are typically **guarded** (`if AIP.Foo and AIP.Foo.Bar then`) because a module may be absent or load later.

### Layers (matches directory + `.toc` order)
- **`core/`** — foundation. `Utils.lua` (string/table helpers, `Utils.Events` pub/sub, `Utils.DelayedCall`), `Parsers.lua` (chat-message parsing: roles, GearScore, raid IDs, spec codes), `DataBus.lua` (inter-addon network protocol), `Core.lua` (DB defaults, central event frame, slash commands, the auto-invite decision logic).
- **`data/`** — live data acquisition. `ChatScanner.lua` (scans chat into LFM/LFG entries; prune lifetime comes from `AIP.db.cacheDuration` via `CS.GetExpiry()`), `InspectionEngine.lua` (player gear inspection + caching), `RaidComposition.lua` (raid templates, role/buff coverage — the largest data file). Composition templates are surfaced through a **gear-tiered variation** system: `Comp.ContentTiers`/`Comp.GetTierInfo` map each template to GS/iLvl ranges, `Comp.GetTemplateVariations` generates Entry/Standard/Farm splits (healers scale inversely with gear), and `Comp.GetRecommendations` couples role gaps with missing raid buffs + mandatory classes. `Comp.RaidBosses` also feeds boss-name → zone detection used by `RaidSessionManager` to stamp accurate loot-history zones (incl. 5-man dungeons). `ItemScore.lua` (`AIP.ItemScore`) is the Pawn-style item scorer + WotLK cap engine: per-archetype stat weights (`IS.Scales`, mapped from class+primary-tree via `IS.SpecArchetype`/`IS.PlayerArchetype`), `GetItemStats`→canonical-stat extraction (+ hidden-tooltip weapon-DPS scan), and cap-aware scoring (`IS.Score` clamps hit/expertise/ArP past their caps so it never over-values a sidegrade). Users override defaults with a pasted Pawn string (`IS.ImportPawn` → `AIP.db.statScales`). Five **character-optimization data files** feed the redesigned Character panel (all follow the BiSData conventions — archetype-keyed tables, a `PLAYER_LOGIN`/`ENTERING_WORLD` `GetItemInfo` warm-cache, `ForArchetype`/`ForSlot`/`ForPlayer` accessors — and are **name-guarded at render** so an unverified/stale id degrades to the correct item NAME as text, never a wrong item): `GearUpgrades.lua` (`AIP.GearUpgrades` — per-archetype, per-inventory-slot upgrade progression `{name,itemID,ilvl,boss,zone,dropChance}`; honest hybrid drop chance — a real datamined % or `nil` + source text, never a guessed number), `GemData.lua` (`AIP.GemData` — "Warmane best-stat" gem plan: per-archetype green→blue→epic cut tiers + a meta gem with its activation note; `mods` use the `ITEM_MOD_*_SHORT` keys the stat panel reads so a picked gem feeds the what-if with zero mapping), `EnchantData.lua` (`AIP.EnchantData` — per-archetype per-slot recommended enchant `{name,itemID|spellID,kind,source,mods}`; proc enchants carry no `mods`), `Consumables.lua` (`AIP.Consumables` — per-archetype flask/food/drink links, shown in the Spec guide), and `GlyphData.lua` (`AIP.GlyphData.LinkFor(name)` — recommended-glyph display-name → inscription itemID so `SA.Guide` glyphs render as real links). Each also has a `BySpec` override table (keyed by `SG.KeyFor()`) for specs an archetype default is too coarse for.
- **`modules/`** — feature logic, mostly headless: `Queue`, `Waitlist`, `Blacklist`, `Promote`, `MessageComposer`, `RosterManager` (`AIP.Roster`), `RaidSessionManager` (`AIP.RaidSession`), `Integrations` (`AIP.Integrations` — GearScore addon, lockouts, Blizzard Raid Browser), `Updater` (`AIP.Updater`), `TestData`, the **RaidTools** suite, and the **interop/coaching** modules (below).
  - **Interop/coaching modules** (added as the "enhance-don't-duplicate" suite; all read-only or opt-in): `DBMBridge` (`AIP.DBMBridge`) speaks DBM's `D4` addon-message protocol — listens for `PT`/`BT`/`CR` and renders the raid's pull/break/combat-res on AIP's own timer bars, and `/aip pull`/`/aip break` broadcast the same format so DBM users see it on their native bar (leader-gated via `RT.CanBroadcast`). `ThreatCoach` (`AIP.ThreatCoach`) uses native `UnitDetailedThreatSituation` (present since 3.0.2 — no Omen dep) to warn you before you pull aggro (opt-in `AIP.db.threatCoach`, instance+combat gated). `Readiness` (`AIP.Readiness`) is the pre-pull self-check (flask/food/durability/talents/glyphs/tank-Defense-540) run by `/aip check` and auto-fired on `READY_CHECK`. NOTE: `RT.StartTimer` is deliberately NOT gated on `mechanicAnnounce` (the mechanic callers gate themselves) so pull/break bars and `/aip timertest` render regardless.
  - **Character-optimization suite** (built on `AIP.ItemScore`): `GearAdvisor` (`AIP.GearAdvisor`, `/aip gear`) ranks best-from-your-bags upgrades + audits missing enchants (item-string field 2 == 0) and empty gem sockets (hidden-tooltip `EMPTY_SOCKET_*` scan), and **peer-shares** each user's own audit summary over a new DataBus `GEAR` event so the raid sees enchant/gem/score status with **zero inspection** (`/aip gear raid`). `UpgradePath` (`AIP.UpgradePath`, `/aip upgrade`) reports weakest slots (by iLvl), an avg-iLvl progression recommendation, curated raid-level source hints, and scores a shift-clicked item vs equipped. `SpecAdvisor` (`AIP.SpecAdvisor`, `/aip spec`) reads the build via `GetTalentInfo`, shows the verified recommended split/glyphs (`SA.Guide`), and imports a Wowhead digit-string to diff and one-click *learn missing points* (`LearnTalent` loop; can't remove misplaced points — needs a trainer reset). `PostPull` (`AIP.PostPull`, `/aip postpull`) prints a post-boss report reading DPS/HPS from Skada (`Skada:GetSet`) or Recount (`Recount.db2.combatants`) with avoidable-damage/interrupt/dispel tallies from the combat log. `Rotation` (`AIP.Rotation`, `/aip rotation`) is the live **next-ability advisor + DPS overlay**: `APLS[class][primaryTree]` are per-spec priority lists whose `pick()` reads live cooldowns (`GetSpellCooldown`), resources (`UnitPower`/`GetComboPoints`), procs/DoTs (`UnitBuff`/`UnitDebuff` caster-filtered), and target HP; the movable `AIPRotationOverlay` shows the recommended spell's icon plus **two icon bars** — an INSTANT bar (`R.ActiveProcs` → every active instant-cast proc, pulsing green) and a COOLDOWNS bar (`R.SpecCDs` = per-spec `CDS` + DPS racial; each icon bright+glow when off cooldown, desaturated with a countdown when on CD) — and a live DPS number (own combat-log damage / active time). Advisory only (never casts — protected on 3.3.5a). Surfaced as the panel's **Combat** section (spec priority list + live DPS) and the "Rotation" toggle. `APLS` now covers **all 30 class/spec combos** (healers/tanks get a priority/triage list). `data/BiSData.lua` (`AIP.BiSData.ForArchetype`) is a curated ICC-endgame BiS chase-list per archetype (item + boss/raid source + how-to-obtain), shown in the Upgrades section. The Character panel also embeds a rotatable **3D `PlayerModel`** (`SetUnit("player")`, drag-to-rotate/scroll-to-zoom) in a right-hand column; imported talent builds are stored **per spec** (`AIP.db.recBuilds[SpecKey]`) so a diff never compares across specs. The **Spec** section renders a real **talent-tree graphic** (`renderTalents`): three trees of `GetTalentInfo` icons positioned by tier/column, rank shown, coloured by learned/maxed, `GameTooltip:SetTalent` on hover, with a build/recommended/how-to-play guide beneath — it swaps in over the scroll list (`frame.talentPane` shown, `frame.scroll` hidden). The Character tab is now **3 sections** (Gear/Spec/Rotation — Readiness was dropped, its stat-caps + prep checks + new consumable links folded into the Spec guide sidebar). **Gear is a Blizzard-style paperdoll** (`ensureGearPane`/`renderPaperdoll`): equipped-slot icon Buttons in two columns flanking the 3D model, each with a quality border + E/G marker (`GA.SlotAudit`) and a `SetInventoryItem` tooltip; clicking a slot (`selectSlot`) opens a central detail scroll (`renderSlotDetail`/`buildSlotDetail`) listing equippable **upgrade items** (source + honest drop chance + iLvl, from `GearUpgrades`), **recommended gems** (green→purple, from `GemData`) and the **recommended enchant** (from `EnchantData`) — each a checkbox that feeds the live what-if via a generalized `entry.sel` selection descriptor (`frame.detailSel`, summed in `updateStats` as flat `mods` for gems/enchants + item-delta for upgrades). No slot selected → the old Gear summary (`buildGear`: ENCHANTS & GEMS → UPGRADES IN YOUR BAGS → WHERE TO GO NEXT). `selectSection`/`P.Update` three-way-switch Gear→gearPane / Spec→talentPane / Rotation→`frame.scroll`; the paperdoll refreshes on section-enter / `P.Update` / `PLAYER_EQUIPMENT_CHANGED` only (never the 0.5s ticker — `SlotAudit` is a heavy ×17 tooltip scan). **Spec** renders the talent trees on the left with a scrollable guide **sidebar** on the right (build/recommended, glyphs as real links via `GlyphData.LinkFor` tinted have/missing, live stat-cap status, prep checks, consumable links, variations). **Rotation** rows carry optional `header`/`accent` fields (`renderContent` draws a bar bg + coloured left stripe per group) so single-target/AoE/DoTs/procs/how-to-play read as scannable lanes. `renderContent(entries, content, scroll)` is now generalized (defaults to `frame.content`/`frame.scroll`) so the same row pool renders the main list, the slot detail, and the Spec sidebar. Historically Gear+Upgrades were merged into one `buildGear`. The rotation **overlay** (`Rotation.lua`) has a **DOTS ON TARGET** bar: `R.SpecDoTs()` scans `SG.DoTs[key]` auras (player-applied only) on the target/self, shows seconds-left, and desaturates + red-pulses with "!" the instant one drops. Plus a FREE-INSTANT-READY bar and COOLDOWNS bar. The **Rotation section** shows single-target + AoE priority (icons via `SG.AoE`), DoTs-to-keep (`SG.DoTs`), free-instant procs (`SG.Procs`), and a foolproof "how to play" guide (`SG.Rotations`). The **Spec section** (`renderTalents`, below the talent trees) shows stat targets + build variations (`SG.Variants`). The debuff/mechanic announcers now default **ON** (Core.lua) with a one-time `announceDefaultsFixed` migration.
The old **Rotation** notes: live next-ability + active proc + priority + a foolproof "how to play" guide (`data/SpecGuides.lua` → `SG.Rotations[SG.KeyFor()]`, Icy-Veins-sourced per-spec bullets). The **Readiness** section shows prep checks PLUS per-spec **stat-cap status** (`SG.Caps` — hit/expertise/ArP-1400/Defense-540 targets vs live `GetCombatRating`, capped/UNDER). A **Character Stats** box under the 3D model shows role-aware live stats with a **what-if projection**: ticking an upgrade's checkbox in the Gear list (`whatif` field on the entry → `frame.whatif`) sums `GetItemStats` deltas vs the replaced slot and shows `current -> projected` (rating stats show `+N`). Bag upgrades also carry a **cap-contribution** note (`capContribution` → which of Hit/Exp/ArP/Defense/Haste/Crit the item feeds). The footer **Import** box is one smart auto-detecting field (Wowhead talent link vs Pawn scale) + Apply-talents. **Pull/Break live on the title bar** (via `titleBtn`, visible when minimized) alongside a **Tools** shortcut (`RaidTools.ToggleRollWindow`). Header GS uses `AIP.CentralGUI.CalculatePlayerGS()` (same source as the footer, matches GearScore addon). `GearAdvisor.BestFromBags` is hand/dual-wield aware (never suggests a 1H for a 2H user's off-hand). BiS is a **sequential per-slot progression** (`BiSData.List[arch]` = ordered `{slot, chain={{name,itemID,source,note}}}`, stepping-stone → BiS); each item renders as a real item link (web-verified 3.3.5a raid/vendor IDs, name-guarded via the item's leading `%a+` so a stale ID falls back to text — never shows a wrong item). Tier-vendor pieces use a **class-aware** map `BiSData.Tier[class][archetype] = {setName, headID, chestID}` with **ORIGINAL-3.3.5a IDs** (511xx range, verified on cavernoftime.com/rising-gods.de — NOT the 2022-Classic 512xx renumbering; wowhead.com/wotlk and wotlkdb.com both serve the Classic IDs and must be avoided for these). `B.Tier[class][arch] = {setName, head, shoulders, chest, hands, legs}` (all 5 verified 511xx IDs); `B.TierFor` resolves the player's own set and the Upgrades tier slot renders **all 5 pieces** as individual name-guarded links (labels from `B.TierSlots`). Priest/Druid tier IDs still text-only (unverified). BiS weapon chains are progressions (entry -> raid BiS -> ultimate), e.g. strDPS 2H = Bryntroll(264) -> Glorenzelg(284) -> Shadowmourne; Hunter ranged = Zod's(264) -> Fal'inrush(284, true BiS). Section builders return row entries; `Spec` is the exception (talent graphic, not rows) so `selectSection`/`P.Update` special-case it. **Data honesty:** stat weights and talent splits are curated/verified; exact per-talent strings and item-ID loot tables are NOT fabricated (paste a Pawn/Wowhead string instead), so coverage is extensible without shipping wrong data. **GUI:** all of this is surfaced in the new **Character** tab — `ui/panels/CharacterPanel.lua` (`AIP.Panels.Character`, standard `Create(container)`/`Update()` panel), registered in `GUI.Tabs` with its container `Create` call and `Update` dispatch wired in `CentralGUI.lua`. The panel has a summary header, highlighted section nav (Gear/Upgrades/Spec/Readiness), and renders the modules' data-returning functions into **icon rows inside a PLAIN (un-templated) `ScrollFrame` + mousewheel** (item rows show the game tooltip on hover via `GameTooltip:SetHyperlink` and shift-click to link), plus Pawn/Wowhead import boxes and the coaching toggles. **Gotcha:** do NOT build a scroll frame from `UIPanelScrollFrameTemplate`/`FauxScrollFrameTemplate` (incl. `UI.CreateScrollList`/`UI.CreateMultiLineEditBox`) with a nil name — the template scrollbar's `OnLoad` does `self:GetName()..".."` and hard-errors on an unnamed frame; those factory helpers now pass a generated name (`uname()`), and the Character panel sidesteps templates entirely. **Seamless Blizzard-UI hooks** live in `modules/GearHooks.lua` (`AIP.GearHooks`): it `HookScript("OnTooltipSetItem")` on `GameTooltip`/`ItemRefTooltip` to append an AIP score + upgrade/downgrade % to **every** item tooltip (Pawn-style, via `IS.UpgradeInfo`), and overlays enchant (E) / empty-socket (G) markers on the character-sheet slot frames (`Character*Slot`) driven off `PLAYER_EQUIPMENT_CHANGED` + `CharacterFrame` OnShow, using `GA.SlotAudit(slot)`. Both opt-out via `AIP.db.tooltipScore` / `AIP.db.paperdollAudit`.
  - **`Updater`** (`AIP.Updater`) is version-check-only — WoW addons are sandboxed (no HTTP, no file I/O) so it *cannot* self-update. It learns the latest version from other AIP users' `AIP.Version` broadcast over the DataBus PING/PONG, compares with dotted-numeric `U.IsNewer`, and shows a one-time inline notice + a Settings-panel banner with the GitHub releases link. `U.Current()` reads `AIP.Version` (the `Core.lua` constant), so keep that constant current or the checker mis-reports.
  - **RaidTools** (`AIP.RaidTools`, alias `RT`) is one logical module deliberately split across four `.toc` entries — `RaidTools.lua` (chat sender + state + most feature logic), `RaidToolsRoll.lua` (roll countdown/capture/winners), `RaidToolsUI.lua` (roll window, announce config, floating bar, countdown timer-bar widget), `RaidToolsEvents.lua` (its own event frame). The split exists so the WoW load phase reports failures per-section. All four files share state via `RT = AIP.RaidTools` (each does `AIP.RaidTools = AIP.RaidTools or {}`), so the `RaidTools.lua` constants/state block must load first. `RT.Send(msg, channel)` is the smart channel-fallback sender (RAID_WARNING→RAID→PARTY→SAY based on group state and leader/officer rank) — route raid-tool chat output through it. Loot trade-window timers use `RT.LOOT_EXPIRE_SECONDS` (2h BoP) / `RT.LOOT_WARN_SECONDS`.
    - Beyond loot/rolls, `RaidTools.lua` hosts the **raid-assist subsystems**, all opt-in via `AIP.db` flags: floating-bar shortcuts (Ready Check, buff delegation), the **buff-delegation** assigner (`RT.ClassBuffDuties` — one distinct buff per same-class caster), the **self debuff/curse announcer** (`RT.KnownDebuffs` + a dispel-aware fallback, fired from `UNIT_AURA`), and the **auto mechanic announcer** (`RT.MechanicSpells` boss casts, `RT.BossEmotes`, target/focus boss-health milestones, raid-health) plus **countdown timer bars** (`RT.AbilityTimers` — DBM-WotLK-verified recast intervals; Bloodlust duration/lockout) and **class-targeted mechanic duties** (`RT.MechanicClassDuties` → `RT.EmitClassDuties`; gated by `AIP.db.classDutyAnnounce`, default true — tells the specific classes present what to do with a class ability, e.g. Jaraxxus *Nether Power* → "Mage: Spellsteal!"). `RaidToolsEvents.lua` is what wires these: it registers `CHAT_MSG_SYSTEM` (roll results), `UNIT_AURA` (debuffs), `COMBAT_LOG_EVENT_UNFILTERED` + `CHAT_MSG_RAID_BOSS_EMOTE` + `UNIT_HEALTH` (mechanics), and an `OnUpdate` running a 3s raid-health check and 60s loot-expiry check. Note WotLK has **no `boss1` token / `ENCOUNTER_START`** — boss health is read from `target`/`focus` only (this is how DBM-WotLK does it too).
    - **Two accuracy guards make the announcers "instance-only and correct" — preserve both when editing:** (1) **`RT.InPveInstance()`** (`IsInInstance()` → `party`/`raid`) gates *every* mechanic + debuff entry point (`OnMechanicCombatLog`/`Emote`/`Health`, `CheckRaidHealth`, `ScanSelfDebuffs`). Without it, generic spell/emote names shared with world/trash mobs ("Frenzy", "Fire Bomb", "Frost Breath", "Overcharge", "Vortex") fire call-outs while questing or in town. Mechanic emotes are matched **only** against `CHAT_MSG_RAID_BOSS_EMOTE` (scripted) — never `MONSTER_EMOTE`/`MONSTER_YELL`, which carry trash text. (2) The debuff announcer **only requests another player's dispel when the debuff is actually dispellable**: `debuffDispelVerb(dtype)` returns `nil` for a non-dispel type and encodes the real WotLK friendly-dispel matrix (Magic→Priest/Pala; Curse→Mage/Druid/Shaman; Poison→Druid/Pala/Shaman; Disease→Priest/Pala/Shaman — Shaman's Purge is offensive-only, so *not* Magic). A non-dispellable debuff that merely stacks gets a neutral self-note, never a false "dispel me". Keep mechanic call-outs jargon-free (spell out "line of sight", etc.). (3) **Class duties follow the same rules and add two of their own:** `RT.EmitClassDuties` delivers per `mechanicAnnounceChannel` — `SELF` (or a chat channel while you're *not* leader) shows a personal heads-up for **your own class only**; a chat channel while you *are* raid-leader/assist (`RT.CanBroadcast()`) broadcasts one leader-gated line per present class. Only list a class with a *genuine* 3.3.5a ability for that mechanic, and **never** auto-assign "assigned-dispeller-only" mechanics (Necrotic Plague, Rotface's Mutated Infection) — broadcasting those to every dispeller is a wipe risk, so they're deliberately absent from `MechanicClassDuties` (the debuff announcer instructs the carrier instead).
- **`ui/`** — presentation. `UIFactory.lua` (`AIP.UI` — reusable widget builders, use these instead of hand-rolling frames), `TreeBrowser.lua`, `CentralGUI.lua` (the main window controller — by far the biggest file), `CompositionUI`, `MainUI`, and `ui/panels/*` which register into `AIP.Panels.*`.

### Two event systems — know which to use
1. **Central dispatcher in `core/Core.lua`**: one `CreateFrame` event frame with a big `OnEvent` switch handling `ADDON_LOADED`, `CHAT_MSG_*`, roster changes, etc. This is where chat triggers flow into `ProcessMessage` → smart-condition checks → `InvitePlayer`/`AddToQueue`. Core chat/invite behavior lives here.
2. **`AIP.Utils.Events` pub/sub** (`Register`/`Unregister`/`UnregisterAll(owner)`/`Dispatch`): a lightweight wrapper letting any module subscribe to WoW events without its own frame. Prefer this for module-local event needs; pass an `owner` so `UnregisterAll` can clean up.

### Saved-variables lifecycle
`core/Core.lua` owns the `defaults` table and `DB_VERSION`. On `ADDON_LOADED` it deep-merges `defaults` into `AutoInvitePlusDB` (only filling missing keys, preserving user settings), bumps `dbVersion`, and assigns `AIP.db = AutoInvitePlusDB`. **When you add a persisted setting, add it to `defaults` in `Core.lua`**; bump `DB_VERSION` only when a structural migration is required (the comment on the `DB_VERSION` line tracks the history).

### UI panel convention
Panels in `ui/panels/` set `AIP.Panels = AIP.Panels or {}` then define `AIP.Panels.<Name> = { Create(container), Update() }`. `CentralGUI` lazily calls `Create` once per tab into a container frame and calls `Update` on tab switch. New tabs are wired in `GUI.Tabs` and the init/switch logic in `CentralGUI.lua`.

### DataBus (inter-player comms)
`core/DataBus.lua` is a small protocol over WoW addon messages + a hidden chat channel (`AIPSync`) for cross-guild reach. It defines typed events (`LFM`, `LFG`, `PING`, `PONG`) with fixed field lists in `DB.EventTypes`, serializes them under the `!A:` chat prefix, and rate-limits/prunes. Other addon users running AIP see each other's LFM/LFG broadcasts. Messages on the DataBus channel are filtered out of normal chat processing in `Core.lua`.

## WotLK / Lua 5.1 constraints (important)

This targets a 2010-era client. Modern WoW/Lua APIs do **not** exist:
- **No `C_Timer`** — use `AIP.Utils.DelayedCall(delay, func)` (a one-shot `OnUpdate` frame). This pattern recurs throughout for staggered/delayed sends.
- **Lua 5.1 only** — no `goto`, no integer division operators, etc. Use `strsplit`, `strtrim`, `string.format`, `table.insert`.
- **Polyfills** live at the top of `core/Utils.lua` (e.g. `string.trim`). Don't assume newer string methods exist.
- **WoW global API** (`SendChatMessage`, `InviteUnit`, `GetChannelName`, `GetGuildRosterInfo`, `IsRaidLeader`, …) is the standard library here. Channel IDs vary by server, so the code resolves channels by name match (`FindChannelId`) rather than hardcoded IDs.
- **Chat throttle avoidance is a real design concern**: spam sends are staggered with per-channel cooldowns, and `Core.lua` watches `CHAT_MSG_SYSTEM` for squelch/throttle messages (`OnChatBanDetected`) to auto-back-off. Preserve this when touching broadcast code.

## Slash commands

Registered in `core/Core.lua`: `/aip`, `/autoinvite`, `/ai`. `/aip help` lists everything; `/aip` alone opens the central GUI. Subcommands dispatch to module `SlashHandler` functions (e.g. `/aip comp`, `/aip roster`, `/aip databus`). RaidTools is driven by its own subcommands handled inline in `SlashHandler` rather than a module `SlashHandler`: `/aip roll [item]` (start a roll, or toggle the roll window with no arg), `/aip rollwindow`/`/aip rolls`, `/aip rw`/`/aip announceloot` (announce reserved loot), `/aip bar`/`/aip announcebar` (toggle the floating announcement bar), `/aip readycheck`/`/aip rc`, `/aip buffs`/`/aip delegate` (announce buff assignments), `/aip timertest` (preview the countdown timer bars). Composition adds `/aip comp recommend`; `/aip update` (or `updates`) runs the update checker. Add new subcommands to `SlashHandler` and document them in the `help` branch.

## Conventions

- User-facing output goes through `AIP.Print(msg)`; gated debug output through `AIP.Debug(msg)` (only shows when `AIP.db.debug` is set).
- Player names: normalize with `AIP.Utils.NormalizeName` and compare case-insensitively (`:lower()`) — chat delivers inconsistent casing.
- Roles use the four-way split `TANK / HEALER / MDPS / RDPS` (melee/ranged DPS) in composition and DataBus; spec codes and class mapping live in `Parsers.SpecCodeInfo`.
- The codebase favors guarded cross-module calls and reusing `AIP.Utils` / `AIP.UI` helpers (the headers cite "DRY principle" and OOP-ish namespacing) — match that rather than duplicating helpers.
